A Django app implementing a basic library management system. 

Main features:
- CRUD for library resources of different kinds + search bar
- lend/return/prolong mechanics for library resources
- admin, librarian and reader user accounts with login and CRUD, login, mocked change password mechanics

Used tools:
- css styling with bootstrap
- postgresql database
- unit tests with django.test and functional tests with Selenium 
